# Рантайм. Версии закреплены: сборка на хостинге идёт с нуля и без пинов
# подтянула бы свежий мажор FastAPI/pydantic — тесты локально этого не поймают.
# Обновлять осознанно. Инструменты разработки — в requirements-dev.txt.
asyncpg==0.31.0
fastapi==0.137.1
pydantic==2.13.4
uvicorn==0.49.0
# Эмбеддинги проблем (app/embed.py): локальная многоязычная ONNX-модель,
# без ключа. Тянет onnxruntime и numpy; модель (~240 МБ) качается при старте.
fastembed==0.8.0
