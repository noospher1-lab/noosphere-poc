__pycache__/
*.pyc
*.bak
noosphere.db
.env
# копии .env содержат живые ключи; без этой строки любой
# `git add -A` утаскивает секрет в историю
.env.*
.pytest_cache/
uvicorn.log
dialogue_*_turns.json
# стенд агентов: логи прогонов и разговоры с компаньоном — рабочие данные,
# а не код (в них черновики людей, которым не место в истории репозитория)
uvicorn-agents.log
companion-log.jsonl
tools/agents/runs/
tools/agents/*.log

# Идентификаторы боевого развёртывания (проект/сервис/ключ Railway). Не секрет
# сам по себе, но в публичном репозитории это карта инфраструктуры. Образец —
# deploy.env.example, он в гите.
deploy.env
# Мой диалог с Gemini и Claude, из которого генерились варианты ответов
# (tools/gen_variants.py). Рабочий материал автора, не код: черновые
# формулировки и места, где модели галлюцинировали.
tools/dialog.txt
